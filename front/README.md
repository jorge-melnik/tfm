# Agroeco

Se han configurado Path Aliases en el compilador de TypeScript para desacoplar la ubicación física de los archivos de su jerarquía arquitectónica (@core, @shared, @features), optimizando la mantenibilidad y el tiempo de indexación del IDE.
